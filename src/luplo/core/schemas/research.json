{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "research context",
  "description": "Free-form metadata for items.context when item_type='research'. The research URL lives in items.source_url (NOT NULL enforced by DB CHECK) and the cache expiry in items.expires_at — not in context. additionalProperties=true (P6).",
  "type": "object",
  "properties": {},
  "additionalProperties": true
}
